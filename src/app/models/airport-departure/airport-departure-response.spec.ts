import { AirportDepartureResponse } from './airport-departure-response';

describe('AirportDepartureResponse', () => {
  it('should create an instance', () => {
    expect(new AirportDepartureResponse()).toBeTruthy();
  });
});
